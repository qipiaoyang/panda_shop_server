// default config
module.exports = {
  // 可以公开访问的Controller
  publicController: [
    // 格式为controller

  ],

  // 可以公开访问的Action
  publicAction: [
    // 格式为： controller+action

    '/admin/auth/login',
  ]
};
